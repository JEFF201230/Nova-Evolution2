# RUNTIME NOVA — Matrice finale de certification

Mission : `RUNTIME-NOVA-FINAL-CERTIFICATION-AUDIT-001`
Date d'audit : 2026-07-25
Révision auditée : `d54cf2adaacfc68aa5ad2c30ae4db5d83b7ecdd7` avec worktree préexistant non propre

Règle appliquée : une anomalie n'est `CLOSED` que si la correction est présente dans le chemin actif, effectivement branchée, couverte par un test pertinent et sans vérité concurrente.

| Anomalie | Cause initiale | Preuve code | Preuve test | Risque résiduel | Statut |
|---|---|---|---|---|---|
| NRA-001 | Approbation/certification finale non authentifiée et non liée au rapport. | `nova-core.http.ts:188-209` authentifie `/certify`; `mission-certification.ts:29-98` signe la liaison. Mais `nova-core.http.ts:215-219` conserve `/approve` sans authentification et `nova-core.service.ts:259-263` réémet le certificat existant. | HTTP : autorité absente `401`, mauvais fingerprint `422`, certificat vérifiable. Aucun test de refus de double certification; `/approve` anonyme est testé positivement. | Deux voies finales concurrentes (`ACCEPTED` et `CERTIFIED`); double certification non refusée; `runId` fourni par le client non validé. | PARTIAL |
| NRA-002 | Une exécution PowerShell en échec pouvait produire `ReportSubmitted`/`SUBMITTED`. | `orchestrator-runtime.service.ts:566-579` transforme les terminaisons en `FAILED`, `TIMEOUT` ou `CANCELLED`; `ReportSubmitted` n'est autorisé que depuis `RUNNING` (`:79`). | Tests orchestrateur : FAILED, TIMEOUT et CANCELLED persistés, verrou libéré; tests PowerShell de classification. | La machine d'état globale reste concurrente avec le module canonique, mais ce défaut précis n'est plus reproduit. | CLOSED |
| NRA-003 | Verrou actif laissé après `FAILED`. | `executeMission` appelle `releaseLockIfTerminal` dans le chemin d'erreur (`orchestrator-runtime.service.ts:566-579`). | Test `Failed execution releases the active lock...` et variantes TIMEOUT/CANCELLED. | Les verrous orphelins après crash restent sans recovery actif, traité par NRA-007/NRA-022. | CLOSED |
| NRA-004 | Mutations préparatoires partielles sauvegardées après échec. | `NovaCoreService.mutate` restaure le snapshot avant propagation de l'erreur; les sauvegardes sont sérialisées; `nova-core.store.ts:55-61` écrit puis renomme. | `Une erreur de verrou annule toute l’assignation intermédiaire`, avec réouverture du snapshot. | Atomicité dépendante du comportement de `rename` du système de fichiers, mais la cause initiale est couverte. | CLOSED |
| NRA-005 | Diagnostics Git/spawn/Codex masqués par une erreur générique. | `RuntimeDiagnostic`, `createCommandDiagnostic`, `translateGitPreflightError` et `translateRuntimeError` conservent code, phase, cwd, sorties, runId et correlationId. | Tests d'exception PowerShell 13/13 et tests d'exécution; pas de test HTTP négatif qui vérifie le contrat complet. | Preuve de propagation HTTP incomplète; flux PowerShell de validations susceptible de deadlock avant diagnostic. | PARTIAL |
| NRA-006 | Absence d'observabilité temps réel. | Route SSE active `nova-core.http.ts:106-128`; événements structurés et abonnements actifs dans `orchestrator-runtime.service.ts:286-330`. | Aucun test n'ouvre `/monitor/stream`; les suites HTTP ne testent ni SSE, ni heartbeat, ni déconnexion. | Les événements sont surtout des transitions; stdout/stderr ne sont pas diffusés pendant l'exécution. | PARTIAL |
| NRA-007 | `RUNNING` non durable et aucune reprise contrôlée après crash. | Les runs et événements sont sauvegardés via l'abonnement de snapshot; `getIncompleteRuns` expose les runs `RUNNING`. Aucune route/commande `reconcile`, `resume`, `abandon`, `quarantine` ou `recover`. | Le test de recovery est une classification pure dans un module non branché; aucun test kill/redémarrage du service. | Run interrompu détectable mais non réconciliable; rapport présent/absent et verrou orphelin non traités dans le runtime actif. | OPEN |
| NRA-008 | Rapport choisi par date de modification et non lié au run. | Dossier `runs/<runId>`, recherche bornée au dossier du run et comparaison exacte des hashes/identités dans `nova-core.execution.ts:300-328`. | Test complet `official report with a stale run binding is rejected`; tests de binding run/manifest. | Le cas exact « ancien rapport ailleurs + nouveau run échoué » n'a pas de test dédié, mais l'ancien chemin global par mtime n'est plus utilisé. | CLOSED |
| NRA-009 | Prompt effectif et requête d'exécution non durables. | `prompt.md`, `execution-request.json`, `manifest.json` et `run-binding.json` sont persistés; hashes dans `run-binding.ts`. | Tests de liaison et certificat altéré. | `request.prompt` peut remplacer le contrat sans validation explicite; le test d'altération du prompt est une primitive de certificat, pas le chemin complet d'import. | PARTIAL |
| NRA-010 | Configuration effective Codex non maîtrisée. | PowerShell contrôle une version minimale et choisit `codex.cmd`/`codex`; le binding prévoit chemin/version/hash. L'appel `buildRunBinding` ne renseigne aucun champ Codex et l'exécution n'isole ni configuration utilisateur ni environnement. | Aucun test Codex réel multi-configuration; aucune vérification du hash/path du binaire. | Reproductibilité inter-postes non démontrée; config utilisateur active possible. | OPEN |
| NRA-011 | Absence de timeout, cancel, kill d'arbre et limites de sorties. | Runner TypeScript : AbortSignal, timeout configurable, `taskkill /T /F`, groupe POSIX, SIGTERM/SIGKILL, lecture concurrente et plafond combiné. Mais `Invoke-NovaCoreNamedCommand` lit stdout puis stderr synchroniquement, sans timeout. | Tests TIMEOUT, CANCELLED, arbre de processus et stdout+stderr volumineux passent. Aucun test d'un processus ignorant explicitement le premier signal; aucune couverture du deadlock PowerShell. | Deux moteurs de processus avec garanties différentes; plafond combiné plutôt que deux plafonds séparés. | PARTIAL |
| NRA-012 | Validation déclarative, non dérivée des fichiers modifiés; livrables recopiés. | `validation-matrix.ts` et `NovaCore.Reporting.psm1` sélectionnent les suites depuis le delta Git réel. `mapOfficialReportToMissionReport` recopie encore `mission.deliverables` sans existence/hash/taille/date. | Tests matrice 2/2 et PowerShell reporting; E2E fichier attendu présent/absent. | Preuve matérielle des livrables et rattachement au run incomplets. | PARTIAL |
| NRA-013 | Missions d'audit non forcées en `READ_ONLY`. | `selectProfile` classe AUDIT/INSPECTION/REVIEW/ANALYSIS, mais `request.profile ?? selectProfile(mission)` autorise l'override API. | Test négatif d'audit : profil automatique `READ_ONLY`, manifeste effectif `BUILD`, `changesExpected=true`, bypass accepté. | Contournement direct du profil et de la règle zéro modification. | PARTIAL |
| NRA-014 | Machine d'état et certification non conformes. | `canonical-state.ts` définit une machine, mais n'est importé par aucun chemin actif. Le runtime utilise `MissionState` et `TRANSITIONS` séparés, avec `DRAFT`, `READY`, `LOCKED`, `SUBMITTED`, `ACCEPTED`, etc. | Deux tests seulement sur la primitive canonique; les tests d'orchestrateur valident la machine legacy concurrente. | Double vérité d'état active; séparation technique/validation/certification non unifiée. | OPEN |
| NRA-015 | Snapshot seul, sans journal append-only intègre et autoritatif. | `AppendOnlyJournal` hash-chaîne existe mais n'est importé par aucun runtime actif. Le runtime actif réécrit `runtime.json` et son `journal.events` avec un checksum global, sans `previousHash`, `eventHash` ni `schemaVersion` par événement. | 3 tests du journal isolé; tests migrations du conteneur actif. Aucun test d'intégration entre les deux. | Ruptures/suppressions peuvent être rendues « valides » en recalculant le checksum; dépendance exclusive au snapshot/enveloppe. | OPEN |
| NRA-016 | Preflight Git incomplet et course avant spawn. | `git-preflight.ts` vérifie version, worktree, top-level, HEAD, branche, detached/unborn, status et sous-modules; seconde inspection juste avant spawn et comparaison. | Tests preflight : succès, detached, unborn, sous-module invalide et drift. | Pas de surveillance des changements externes après le spawn, traitée par NRA-017. | CLOSED |
| NRA-017 | Attribution non déterministe; verrous sans recouvrement parent/enfant. | `acquireLock` compare seulement l'égalité via `lockScope.includes(entry)`; `scopesOverlap` existe mais n'est pas appelé par le lock manager. | Test négatif actif : les scopes `server/nova-core/**` et `server/nova-core/http.ts` obtiennent deux verrous actifs simultanés. | Double exécution sur chemins recouvrants et modifications externes pendant run. | OPEN |
| NRA-018 | Sémantique de scope TypeScript/PowerShell incohérente. | Normalisation TypeScript versionnée et segment-aware; PowerShell conserve `-like` et sa propre grammaire. | Tests TS Windows/dossier/glob/conflits; E2E PowerShell allowed/forbidden. Aucun corpus commun partagé. | Divergences possibles pour glob, exact, séparateurs et négations. | PARTIAL |
| NRA-019 | Collisions `safeName` et containment fragile. | `identitySlug` protège les dossiers de run et `assertScopeContainment` protège les frontières; le chemin mission/prompt utilise toujours `safeName(projectId/missionId)`. | Tests slug et dépôt frère préfixé passent sur les primitives. | Collisions encore possibles dans `dataRoot/missions/<safeName>/<safeName>`. | PARTIAL |
| NRA-020 | Payloads API peu validés. | Création valide le scope versionné; body global borné. `/execute`, `/certify`, `/assign` utilisent surtout des casts TypeScript sans schéma runtime strict. | Tests HTTP happy path, autorité absente et fingerprint erroné; pas de corpus de payloads malformés. | Champs supplémentaires ignorés, notamment un `runId` client erroné lors de `/certify`. | PARTIAL |
| NRA-021 | Tests ne couvrant pas les risques critiques. | 47 tests Node et suites PowerShell étendues existent. | Toutes les suites exécutées passent, mais absence de tests SSE, recovery actif, Codex absent/version erronée, double certification, migration interrompue/rollback et journal actif. | Les tests verts couvrent aussi des primitives non branchées et ne prouvent pas la certification du runtime effectif. | PARTIAL |
| NRA-022 | Capacités CEREBRAU exclues sans équivalent NOVA complet. | Pas de dépendance active CEREBRAU, mais journal/replay/recovery ne sont que des primitives isolées; aucune commande recovery active. | Journal 3/3 et classification pure; aucune équivalence bout-en-bout. | Replay durable, attempts, drift et recovery contrôlé absents du chemin actif. | OPEN |
| NRA-023 | Bootstrap non reproductible, Node non imposé, installation couplée au démarrage. | `engines.node >=22` et `assertNovaBootstrap` contrôlent Node; `package-lock.json` v3. `DEMARRER_NOVA.bat` lance encore `npm install`, continue avec avertissement si Git/Codex absents et couple installation/démarrage. | Tests Node bootstrap 2/2; `npm ci --ignore-scripts --offline` sur copie jetable passe et conserve le lockfile. Pas de tests Git/Codex absents. | Parcours utilisateur non reproductible et échec tardif du moteur. | PARTIAL |

## Synthèse

| Statut | Nombre |
|---|---:|
| CLOSED | 5 |
| PARTIAL | 12 |
| OPEN | 6 |
| Total | 23 |

Anomalies P0/P1 non fermées : `NRA-001`, `NRA-005`, `NRA-006`, `NRA-007`, `NRA-009`, `NRA-010`, `NRA-011`, `NRA-012`, `NRA-013`, `NRA-014`, `NRA-015`.

Cette matrice interdit un `GO FOR CERTIFICATION`.
