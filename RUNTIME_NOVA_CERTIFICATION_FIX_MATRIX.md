# RUNTIME-NOVA-CERTIFICATION-CLOSURE-001 — Matrice des corrections

| NRA | Cause issue de l'audit | Correction minimale | Tests exécutés | Résultat | Risque résiduel |
|---|---|---|---|---|---|
| NRA-001 | `/approve` anonyme, double certification acceptée, runId client ignoré. | Voie `/approve` neutralisée en 410 ; runId/fingerprint obligatoires ; double certification en 409 ; autorité HMAC inchangée. | HTTP : autorité absente, mauvais runId, mauvais fingerprint, double appel, certificat vérifiable. | CLOSED | Aucun chemin final anonyme actif. |
| NRA-005 | Preuve HTTP des diagnostics incomplète ; validation PowerShell sujette au deadlock. | Diagnostics structurés conservés et expurgés jusqu'à HTTP ; lecture concurrente des deux flux PowerShell. | HTTP négatif vérifie stdout, stderr, cwd, runId, correlationId ; PowerShell gros flux. | CLOSED | Sorties bornées et expurgées. |
| NRA-006 | SSE non testé et aucune sortie processus live. | `ProcessOutput` branché au journal/observabilité ; stdout/stderr live redacted vers SSE. | Test HTTP ouvre le stream pendant l'exécution et observe stdout et stderr en phase OUTPUT. | CLOSED | Heartbeat reste un mécanisme de transport, sans effet sur l'état. |
| NRA-007 | Runs `RUNNING` détectables mais aucune action de recovery active. | Actions reconcile/resume/abandon/quarantine/recover, route authentifiée, classification rapport/verrou et libération contrôlée. | Snapshot RUNNING redémarré : rapport absent, présent, verrou orphelin, abandon et replay ; recovery HTTP authentifié/non authentifié. | CLOSED | Aucune reprise automatique ; choix d'autorité obligatoire. |
| NRA-009 | Un prompt API pouvait remplacer le contrat généré. | Le prompt personnalisé est refusé s'il ne contient pas intégralement le contrat NOVA ; prompt/requête/manifest/binding restent persistés et hashés. | Test négatif `NOVA_CORE_PROMPT_CONTRACT_MISSING` ; tests de binding stale. | CLOSED | Une extension de prompt reste possible sans retirer le contrat. |
| NRA-010 | Version/path/hash/config Codex absents du binding ; config utilisateur active. | Inspection du binaire, version minimale, SHA-256 et chemin liés au run/rapport/certificat ; `--ignore-user-config`, arguments de profil explicites et strict config. | Binding/certificat Codex ; version 0.143.9 refusée, 0.144.1 acceptée ; E2E PowerShell avec binding exact. | CLOSED | Auth Codex reste fournie par l'environnement, pas par la configuration fonctionnelle. |
| NRA-011 | Runner PowerShell secondaire sans timeout, kill ni lecture concurrente sûre. | `ReadToEndAsync` concurrent, timeout configurable, `taskkill /T /F`, limites stdout et stderr. | PowerShell : deux flux >1 Mo tronqués et timeout avec arbre tué ; Node : TIMEOUT, CANCELLED, arbre et plafond. | CLOSED | Timeout par défaut de validation : 600000 ms, configurable. |
| NRA-012 | Livrables recopiés sans preuve matérielle. | Evidence issue du delta Git réel : existence, taille, mtime, SHA-256 et runId ; revalidation avant certificat. | Fichier valide, preuve absente/hash incohérent, livrable modifié après run. | CLOSED | Les libellés métier restent descriptifs ; les fichiers produits sont autoritatifs. |
| NRA-013 | Override API de READ_ONLY vers BUILD. | READ_ONLY automatique non surchargeable ; payload strict ; `changesExpected=false` dans le manifeste effectif. | Test négatif AUDIT + BUILD ; PowerShell Governance READ_ONLY. | CLOSED | Aucun override API autorisé. |
| NRA-014 | Machine canonique isolée et seconde machine legacy active. | Canonical-state importée dans l'EventBus comme porte autoritative ; mappings détaillés renommés projections ; projection API canonique exposée. | Transitions orchestrateur, bypass vers CERTIFIED refusé, certification HUMAN→COMPLETED→CERTIFIED, replay. | CLOSED | États détaillés conservés uniquement pour compatibilité, sous contrôle canonique. |
| NRA-015 | Journal append-only isolé ; runtime actif protégé seulement par checksum global. | Chaque événement actif est hash-chaîné ; vérification avant replay ; store refuse troncature/réécriture ; projection reconstruite depuis le journal. | Corruption, événement manquant, séquence dupliquée, replay après restart, projection corrompue. | CLOSED | Un attaquant administrateur du disque reste hors modèle de corruption accidentelle/runtime. |
| NRA-017 | Lock manager comparait uniquement l'égalité des scopes. | `scopesOverlap` segment-aware appliqué à chaque paire de scopes actifs. | Test parent `server/nova-core/**` contre enfant `server/nova-core/http/**`; dépôt frère préfixé. | CLOSED | Modifications externes après spawn restent détectées par le delta Git final. |
| NRA-018 | Grammaires TypeScript et PowerShell divergentes. | Normalisation des séparateurs et matcher regex exact/glob/?/** côté PowerShell alignés sur les règles TS. | Corpus de parité dans Reporting ; tests TS Windows, glob, exact, parent/enfant et contradictions. | CLOSED | Deux langages implémentent la même grammaire, surveillée par non-régression. |
| NRA-019 | `safeName` encore utilisé pour les dossiers mission. | `identitySlug` lisible avec suffixe SHA stable pour projectId et missionId. | Test de collisions de noms sûrs et chemins attendus du manifeste. | CLOSED | Longueur lisible bornée, unicité portée par le hash. |
| NRA-020 | Payloads HTTP validés par casts TypeScript seulement. | Schémas runtime stricts, types/tailles/valeurs contrôlés et champs inconnus refusés pour create/assign/evidence/execute/certify/recovery. | Happy paths et champ inconnu 400 ; runId/fingerprint invalides ; profil invalide. | CLOSED | Pas de bibliothèque de schéma externe ajoutée. |
| NRA-021 | Lacunes sur SSE, recovery, Codex, double cert, migrations et journal actif. | Les preuves manquantes ont été intégrées aux 47 tests Node et aux 33 tests PowerShell sans test-placeholder. | Toutes les campagnes listées dans le rapport de régression. | CLOSED | Aucune suite annoncée n'est simulée ou omise. |
| NRA-022 | Journal/replay/recovery sans équivalent NOVA bout-en-bout. | Journal actif autoritatif, replay intègre, attempts, recovery gouverné et routes actives ; aucune reprise automatique. | Crash RUNNING, rapport présent/absent, lock orphelin, abandon, recover, replay et restart persistant. | CLOSED | Reprise volontairement explicite. |
| NRA-023 | Launcher avec `npm install`, prérequis Git/Codex non bloquants. | Installation séparée `npm ci`, Node >=22, Git/Codex obligatoires, aucun install implicite au démarrage. | Tests bootstrap Node/Git/Codex/version et inspection du batch ; contexte/runtime PowerShell. | CLOSED | L'installation reste une étape opérateur explicite. |

## Synthèse

| Statut | Nombre |
|---|---:|
| CLOSED | 23 |
| PARTIAL | 0 |
| OPEN | 0 |

Cette matrice ne constitue pas une décision de certification.

